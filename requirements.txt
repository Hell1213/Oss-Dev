# Core Dependencies
click>=8.0.0
openai>=1.0.0
pydantic>=2.0.0
rich>=13.0.0
tiktoken>=0.5.0
platformdirs>=3.0.0
tomli>=2.0.0
ddgs>=9.0.0
fastmcp>=0.9.0

# OSS Development Dependencies (Phase 0+)
GitPython>=3.1.0
# Note: GitHub CLI (gh) should be installed separately via package manager
# For Ubuntu/Debian: sudo apt install gh
# For macOS: brew install gh
